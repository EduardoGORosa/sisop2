Para compilar:
  (Dentro do dir build)
  cmake ..
  make

Para rodar server:
  (Dentro do dir build)
  ./server [ip] [porta]

Para rodar cliente:
  (Dentro do dir build)
  ./client [user] [ip] [porta]
